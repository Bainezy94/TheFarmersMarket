
<h1>Orders</h1>

<table>
  <thead>
    <tr>
      <th>Farmer</th>
      <th>Profile</th>
      <th>Product</th>
      <th>Volume</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.farmers_profile.stall_name %></td>
        <td><%= order.profile %></td>
        <td><%= order.product %></td>
        <td><%= order.volume %></td>
        <td><%= link_to 'Show', order %></td>
        <td><%= link_to 'Edit', edit_order_path(order) %></td>
        <td><%= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order', new_order_path %>
